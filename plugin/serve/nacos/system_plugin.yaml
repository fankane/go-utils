plugins:
  serve:  # 插件类型: 服务类
    nacos: # 插件名
      default:
        end_point: "localhost:8848"
        namespace_id: "xxx"
        timeout_ms: 5000
        username: "xxx"
        password: "xxx"
        data_id: "xxx"
        group: "xxx"
        not_load_cache_at_start: true