plugins:
  database:  # 插件类型
    mongo: # 插件名
      default:                # 连接名称：default，可以是其他名字
        host: "localhost"
        port: 27017
        user: ""
        pwd: ""
        connect_timeout_ms:  # 连接超时时限，单位：毫秒，不填默认 10s